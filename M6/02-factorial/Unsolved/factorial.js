// Write code to create a function that returns the factorial of `num`

var factorial = function(num) {
  // set up a variable to hold the product of the factorial

  //then set up a loop to going from 1 and ending at num
    //multiply the product by the current number in the loop
    
};
